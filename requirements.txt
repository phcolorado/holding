Django>=4.2,<5.0
openpyxl>=3.1.0
Pillow>=10.0.0
django-widget-tweaks>=1.5.0
django-simple-history>=3.7.0
ofxparse>=0.21
django-axes>=6.0
whitenoise>=6.6
gunicorn>=21.2
# Driver PostgreSQL incluído por padrão: "pip install -r requirements.txt" é
# suficiente para subir com Postgres em qualquer deploy (com ou sem Docker).
psycopg2-binary>=2.9
